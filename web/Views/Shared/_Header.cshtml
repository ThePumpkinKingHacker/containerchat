<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <a class="navbar-brand" href="#">Container Chat</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav"
    aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
            <li class="nav-item
            @if (ViewBag.Link == "Chat")
            {
                @:active                            
            }                        
            ">
                <a class="nav-link" asp-action="Index" asp-controller="Chat">Chat</a>
            </li>
            @if (User.IsInRole("Administrator"))
            {
            <li class="nav-item 
            @if (ViewBag.Link == "Users")
            {
                @:active                            
            }
            ">
                <a class="nav-link" asp-action="Index" asp-controller="User">Users</a>
            </li>
            }
            <li class="nav-item">
                <a class="nav-link" asp-action="Logout" asp-controller="Home">Logout</a>
            </li>
        </ul>
    </div>
    <div class="text-white">
        @User.Identity.Name
    </div>
</nav>
